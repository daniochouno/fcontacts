name: fcontacts
description: A Flutter plugin for accessing to all available data from your phone contacts. Supports iOS and Android.
version: 0.0.1
author: Daniel Martínez <dmartinez@danielmartinez.info>
homepage: https://github.com/daniochouno/fcontacts

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: info.danielmartinez.fcontacts
    pluginClass: FContactsPlugin
